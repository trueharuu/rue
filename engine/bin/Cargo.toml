[package]
name = "bin"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.1"
rayon = "1.12.0"
anyhow = "1.0.102"
engine_core = { version = "0.1.0", path = "../engine_core" }
engine_nav = { version = "0.1.0", path = "../engine_nav" }
engine_search = { version = "0.1.0", path = "../engine_search" }
engine_eval = { version = "0.1.0", path = "../engine_eval" }
