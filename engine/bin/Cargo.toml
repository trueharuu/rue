[package]
name = "bin"
version = "0.1.0"
edition = "2024"

[dependencies]
engine_ai = { version = "0.1.0", path = "../engine_ai" }
engine_core = { version = "0.1.0", path = "../engine_core" }
engine_rng = { version = "0.1.0", path = "../engine_rng" }
engine_nav = { version = "0.1.0", path = "../engine_nav" }
rand = "0.10.1"
rayon = "1.12.0"
anyhow = "1.0.102"
